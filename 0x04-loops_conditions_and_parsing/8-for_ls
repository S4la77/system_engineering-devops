#!/usr/bin/env bash
# This script lists files and prints only the part after the first dash

for file in *
do
    # Skip hidden files
    [ "$file" = "$(basename "$file")" ] || continue
    
    # Remove everything up to the first dash, if any
    echo "${file#*-}"
done
